import IListViewSchema from '../views/List/IListViewSchema';

export const ListSchema : IListViewSchema = {
  id: false,
  name: true,
  language: true,
  classification: true,
  designation: true,
};